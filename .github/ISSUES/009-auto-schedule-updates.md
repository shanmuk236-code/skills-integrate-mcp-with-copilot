Title: Automatic remote schedule updates with configurable refresh interval

Description:
Implement periodic schedule refresh from a configured URL and allow users (or devs) to configure the refresh interval in settings.

Why:
Keeps schedules up-to-date without manual refresh.

Acceptance criteria:
- Background refresh logic or foreground polling with configurable interval.
- Settings UI to select refresh frequency (including disable).

Labels: enhancement
